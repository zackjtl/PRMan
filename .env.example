# 建議：機密分開放——frontend/.env（VITE_*）與 backend/.env（SUPABASE_*、PORT 等）。
# 本檔（repo 根 .env）為選用：後端啟動時仍會先讀取，再由 backend/.env 覆寫。
#
# 自建 Supabase（含 Zeabur 一鍵模板）：
# Zeabur：URL 與 ANON_KEY 請到「Kong」服務 → Variables／Domains（勿僅依 Studio 畫面複製 key）。
# https://zeabur.com/zh-TW/templates/SUPABS
# Google OAuth：在 Zeabur「auth」服務設定 GOTRUE_EXTERNAL_GOOGLE_*；Google Console 的
# 「已授權的重新導向 URI」填：${API_EXTERNAL_URL}/auth/v1/callback
# https://supabase.com/docs/guides/self-hosting/self-hosted-oauth
#
# 若按「Google 登入」後被帶到 Supabase Studio／管理介面：代表 OAuth 完成後導回網址
# 不在允許清單，GoTrue 改用了預設的「網站網址」（常誤設成 Supabase 自己的網域）。
# 請在「auth」服務 Variables 調整（名稱以你模板為準，常見為）：
# - GOTRUE_SITE_URL=http://localhost:5173（本機 PRMan）；上線後改為正式前端網址
# - GOTRUE_URI_ALLOW_LIST 含本機與 callback，例如：
#   http://localhost:5173,http://localhost:5173/auth/callback
#   （若模板支援萬用字元可用 http://localhost:5173/**，勿含空格）
# 改完重啟 auth 服務。說明：https://supabase.com/docs/guides/auth/redirect-urls

SUPABASE_URL=https://your-supabase-api.example.com
SUPABASE_ANON_KEY=your_anon_key
CORS_ORIGIN=http://localhost:5173
PORT=8787
